import 'package:flutter/material.dart';

class HRPage extends StatelessWidget {
  const HRPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('HR Related Forms', style: TextStyle(fontSize: 24)),
    );
  }
}
